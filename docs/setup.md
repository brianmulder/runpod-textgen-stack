# Setup

Install the following tools inside WSL:

```bash
sudo apt-get update && sudo apt-get install -y jq socat shellcheck shfmt openssl
```

Download `runpodctl` from [RunPod](https://github.com/runpod/runpodctl) and place it in your PATH.

Ensure PowerShell is available for certificate installation on Windows.
