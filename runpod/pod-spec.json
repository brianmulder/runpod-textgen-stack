{
  "name": "tg-webui",
  "cloudType": "COMMUNITY",
  "computeType": "GPU",
  "imageName": "atinoda/text-generation-webui:default-nightly",
  "gpuCount": 1,
  "gpuTypeIds": ["$GPU_TYPE"],
  "containerDiskInGb": 40,
  "networkVolumeId": "$VOLUME_ID",
  "volumeMountPath": "/workspace",
  "ports": ["8443/tcp"],
  "dockerEntrypoint": ["bash", "-c"],
  "dockerStartCmd": [
    "apt-get update && apt-get install -y stunnel4 openssl jq && CERT_DIR=/workspace/certs; mkdir -p $CERT_DIR; [[ -f $CERT_DIR/cert.pem ]] || openssl req -x509 -nodes -days 825 -newkey rsa:2048 -keyout $CERT_DIR/key.pem -out $CERT_DIR/cert.pem -subj '/CN=textgen.runpod' -addext 'subjectAltName=DNS:localhost,IP:127.0.0.1'; cat > /etc/stunnel/stunnel.conf <<'EOS'\nforeground = yes\npid =\n[webui]\naccept = 0.0.0.0:8443\nconnect = 127.0.0.1:7860\ncert = $CERT_DIR/cert.pem\nkey  = $CERT_DIR/key.pem\nsslVersion = TLSv1.2\nEOS\nEXTRA_LAUNCH_ARGS='--listen --port 7860 --api --extensions openai --model-dir $MODEL_DIR' /entrypoint.sh & stunnel /etc/stunnel/stunnel.conf"
  ]
}
